{
  "name": "aws-cdk-ephemeral",
  "version": "0.1.0",
  "description": "Tool for easily creating and destroying ephemeral AWS environments using CDK",
  "main": "dist/index.js",
  "bin": {
    "cdkeph": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "aws",
    "cdk",
    "ephemeral",
    "testing",
    "cicd"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.0.0",
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/client-scheduler": "^3.0.0",
    "@aws-sdk/client-sts": "^3.0.0",
    "commander": "^11.0.0",
    "execa": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
